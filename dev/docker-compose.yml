services:
  app:
    build:
      context: ..
      dockerfile: dev/Dockerfile
    volumes:
      - ../src:/app/src
      - ../config:/app/config
      - ../public:/app/public
    ports:
      - "5173:5173"